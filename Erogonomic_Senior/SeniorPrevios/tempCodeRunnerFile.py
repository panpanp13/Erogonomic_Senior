    # max_col_0 = max(a_np[:, 0])  # Max of first column (a[i][0])
                # min_col_0 = min(a_np[:, 0])
                # max_col_2 = max(a_np[:, 2])  # Max of second column (a[i][1]
                # min_col_2 = min(a_np[:, 2])
                # for pt in np.array(land_mark_rula):
                #     if min_col_0<=float(pt[0])<=max_col_0 and min_col_2<=-float(pt[2]) <=max_col_2 :
                #         print('Danger Now')
                #         cimage_array = apply_red_overlay(cimage_array, intensity=0.5)
                #         break